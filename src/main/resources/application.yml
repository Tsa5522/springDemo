server:
    port: 9090

spring:
    datasource:
        url: jdbc:mysql://127.0.0.1:3306/user_db?useUnicode=true&characterEncoding=UTF-8&serverTimezone=Asia/Shanghai&allowMultiQueries=true
        username: root
        password: sf011009sf
        driver-class-name: com.mysql.cj.jdbc.Driver

mybatis:
#    mapper-locations: classpath*:mapper/*Mapper.xml
    type-aliases-package: com.myDemo.demo.entity

#logging:
#    level:
#        org.springframework.security: DEBUG
